# テンプレート移行後のビルド・文書・Release修復 — 実施結果

## 結果

build.py/bundle_html.pyの旧・新処理重複と構文エラーを解消。GUI/CLIビルド成功、C++ engine_tests成功、PySide6テスト75件PASS。カバレッジ対象をsrc.pdf_editorに修正し、JUnitが全件成功のときQt teardownの異常終了コードを成功扱いとするテスト入口を修正。変更後のrun_tests.pyも終了コード0で完了。HTMLバンドルの画像参照修正。CIはproto/testsとnativeを実行し、Release ZIPはdocs/distributionとdist出力に統一、SHA256SUMS.txtにZIPと全バイナリを記録。

## 残件

- QuickDiskRescue / QuickFileCopy / QuickFolderSize / QuickImageView / QuickMarkPDF の変更は未コミット。
- GitHub Actions上の実行とReleaseへの公開は、このローカル検証では確認していない。

## Release 手動実行の整合性

手動実行で入力したタグを `actions/checkout` の `ref` に指定し、そのタグのソースからReleaseアセットを作るよう修正。タグpush時はイベントのrefを使う。
