# PDF Editor プロジェクト仕様書

## 概要
シンプルなデスクトップPDF編集ツール。
複数のPDFファイルを読み込み、ページ単位で柔軟に編集できることを目的とする。
UIは極力シンプルに保つ。

## 対象プラットフォーム
- デスクトップアプリケーション（Windows優先、将来的にmacOS/Linux対応も検討）

## 機能要件（実装済み）

### 1. PDFファイル操作
- 複数PDFファイルの同時読み込み
- 現在の状態を1つのPDFとして保存（実質的な連結＋全編集の反映）
- 選択ページを新しいPDFとして分割保存
- ページ単位での並び替え（QTreeWidgetによるドラッグ＆ドロップ、ファイル間移動対応）
- ページごとの回転（+90° / -90° / 180° の3ボタン）

### 2. ページ編集機能
- ファイルごとにグループ化されたサムネイルツリー（折りたたみ可能）
- ドラッグ＆ドロップによるページ移動（ファイル間移動対応）
- ヘッダー＆フッターの挿入
  - タイトル文字列
  - ページ番号（「- 1 / 12 -」形式）
- プレビューエリア
  - マウスホイールによる拡大縮小
  - 右クリックドラッグによるパン
  - 縦横スクロールバー
  - ページ選択時に横幅フィット表示

### 3. UI / 操作性
- 上部アイコンツールバー（カスタム生成アイコン + ツールチップ）
- サムネイルサイズ切替（小 / 中 / 大）
- 左側：ファイル別サムネイルツリー
- 右側：プレビュー
- 左右ペインの境界線をドラッグしてサムネイル列幅を自由に調整可能（QSplitter）

### 4. その他
- 編集内容の保存（新規PDFとして出力）

## UIレイアウト（現在）
- **上部**: アイコン中心のツールバー（カスタム生成アイコン + テキストアンダーアイコン）
- **左側**: ファイルごとにグループ化されたサムネイルツリー（QTreeWidget、折りたたみ可能、ドラッグ＆ドロップ対応）
- **右側**: 選択ページのプレビュー（ズーム、パン、スクロールバー、選択時fit-to-width）
- **左右境界**: QSplitterによるドラッグリサイズ対応

## サムネイルツリーの仕様詳細
- ファイルヘッダー行：テキスト1行＋上下0.5行パディング（コンパクト表示）
- ページサムネイル：固定幅キャンバス（左右1pxマージン）＋ページ番号ラベル（下部）
  - 縦長ページ：キャンバス高 = 画像高 + ラベル高（標準）
  - 横長ページ：キャンバス高 = 実際の縮小後画像高 + ラベル高（余白なし）
- 折りたたみ状態はドラッグ＆ドロップ後も保持される
- 折りたたまれたファイル内のページは自動展開されない
- 展開ボタン（▶）はインデント20pxの領域に描画

## 技術スタック
- **言語**: Python 3.12+
- **GUI**: PySide6 (Qt6)
- **PDF処理**: PyMuPDF (fitz)
- **画像処理**: Pillow
- **配布**: PyInstaller（予定）

優先観点: 開発速度、PDF操作の強力さ、クロスプラットフォーム性、単一exe配布のしやすさ

## 非機能要件
- UIは極力シンプルに保つ
- 操作は直感的（特にドラッグ＆ドロップ）
- 初回起動時の学習コストを低くする

## 現在のステータス（2026年6月）
- 基本的なPDF閲覧・編集・保存の流れは動作
- サムネイルツリー + ドラッグ＆ドロップ並び替えは実装済み・UX改善済み
- 回転・ヘッダー/フッター・ズーム/パン・サムネサイズ切替は完成
- 分割機能は「選択ページを新しいPDFとして保存」として実装済み
- ツールバーアイコンはPillowで自動生成したシンプルなアイコンを使用
- QSplitterによる左右ペインのリサイズ対応済み
- 横長ページのサムネイルは余白なしで適切な高さに自動調整

## 備考
- 追加機能や詳細仕様は今後、ユーザーの指示に基づいてこのファイルに追記する
- 実装状況と乖離が生じた場合は随時更新する
