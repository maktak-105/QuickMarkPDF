# QuickMarkPDF 変更履歴

このファイルには、公開版の主な変更履歴を記載します。

## バージョン命名規則

- 1桁目の更新（例：`1.0.0`→`2.0.0`）：機能追加
- 2桁目の更新（例：`1.0.0`→`1.1.0`）：バグ修正
- 3桁目の更新（例：`1.1.0`→`1.1.1`）：その他の変更（ドキュメント修正など）

## v3.1.0（2026-08-30）

### バグ修正

- セカンドディスプレイにウィンドウを移動した状態で終了し、そのディスプレイを取り外してから起動すると、メインディスプレイ上にウィンドウが表示されない不具合を修正。保存済みのウィンドウ位置が、現在接続中のどのモニタとも重ならない場合でも無条件に復元されており、アプリ自体は起動している（タスクバーにアイコンは出る）のに、実在しない画面上に描画され続けていた。該当する場合は保存位置を破棄し、既定位置にフォールバックするようにした。
- 既定のウィンドウサイズを1280×820から1024×768に縮小し、小さめのディスプレイでもそのまま収まるようにした。

## v3.0.0（2026-08-29）

### 機能追加

- 右クリックメニューに「テキスト抽出」を追加。全ページ／ページ番号を指定（例:「1,3,5-8」）／現在プレビュー表示中のページのいずれかを選び、「名前を付けて保存」ダイアログで.txtファイルとして書き出せるようにした。テキストは左上から右下へ読む順で抽出する。プレビューのテキスト選択レイヤーが既に使っているpdfiumベースの行矩形抽出（`PdfBackend::get_text_layout`）をそのまま再利用し、同じ視覚行に属する矩形をグループ化して左から右へ連結することで、元のPDFが箇条書き・番号付きリストのマーカーを実テキストとして持つ場合はその行と一緒に保持されるようにした。

## v2.2.1（2026-08-28）

### 機能追加

- PDFプレビュー上のテキストを、pdfiumのテキスト抽出APIで取得した行矩形をもとに透明なテキストレイヤーとして重ね、ブラウザ標準のドラッグ選択・Ctrl+Cコピーを可能にした。既定で有効。右クリックメニューから「範囲選択（画像切り出し）に切り替え」を選ぶと、従来の左ドラッグによるクロップ範囲選択に切り替えられる（ページを開き直すとテキスト選択優先の既定に戻る）。MathJaxの数式やスキャン画像PDFなど、そもそもテキストオブジェクトを持たない部分は選択できない。
- アプリ本体のUIに日本語／Englishの表示言語切替ボタンをメニューバー右端に追加。ツールバー・メニュー・モーダル・ステータスメッセージに加え、保存確認やパスワード入力などのネイティブダイアログも含めて全て切替対応。JS側は`localStorage`、C++側はレジストリに独立して言語設定を保存し、切替のたびに両者を同期する。

### パフォーマンス

- プレビューの描画解像度を固定900px幅から動的化。ページ選択直後はまず900pxで素早く表示し、ズーム操作が止まってから実際の表示ピクセル数に応じて高解像度で再レンダリングするようにし、大きくズームしたときの文字のぼやけを解消した。

### バグ修正

- テキスト選択モードと範囲選択（クロップ）モードを切り替えた際、切替前のモードで選択していた内容（テキストのハイライト、またはクロップ矩形）が画面に残ったままになる不具合を修正。
- サムネイルパネルにフォーカスがあるときの上下矢印キーによるページ切替が効かなくなっていた不具合を修正。「クリックするとスクロールコンテナへブラウザが暗黙的にフォーカスする」という保証のない挙動に依存していたため、サムネイルクリック時に明示的にパネルへフォーカスするよう変更した。
- ウィンドウのタイトルバー・タスクバーにアプリアイコンが表示されない不具合を修正。ウィンドウクラス登録時に`hIcon`/`hIconSm`が一切設定されておらず、exeファイル自体のアイコン（エクスプローラー表示用）とウィンドウのアイコン（タイトルバー表示用）が別物であることに起因していた。

### デザイン

- アプリアイコンを刷新。「PDF」の文字を大きく前面に配置し、Quickシリーズ共通のモチーフとして「Q」の文字を書類アイコンに重ねた。

## v1.2.1（2026-08-27）

### 機能追加

- サムネイルパネルにフォーカスがあるとき、上下矢印キーでページを切り替えられるようにした（従来はパネルのスクロールのみだった）。

### パフォーマンス

- PDFファイルを開く処理（ファイル関連付けからのダブルクリック起動も同じ経路を通る）を高速化した。サムネイル・プレビューを描画するたびにファイル全体を読み直し・再パースしていたのをやめ、開いたままのPDFiumドキュメントをソースファイルごとに使い回すようにした。

### ドキュメント

- バージョン情報ダイアログをQuickシリーズ アプリ仕様に合わせて刷新した。アプリ名・シアン色のバージョン表示・区切り線・「[開発環境]」「[制作者]」の角括弧見出し・制作者の円形ワッペン画像を追加し、カード外周にシアン色の発光境界線を付けた。
- 新しい上下矢印キーのショートカットをアプリ内ヘルプおよび`document/spec.md` / `spec_jp.md`に記載した。

## v1.1.0（2026-08-21）

### バグ修正

- 右クリックのコンテキストメニューが、長いサムネイル一覧の下端付近など画面端で開くとウィンドウ外にはみ出して見えなくなる不具合を修正。ウィンドウ内に収まるよう自動的に位置を補正するようにした。
- MarkdownのPDF書き出しで、見出し（h1〜h6）がダークテーマの明るい文字色のまま印刷用スタイルに上書きされず、書き出したPDF上でほぼ白色（視認不可）になっていた不具合を修正。
- MarkdownのPDF書き出しが、文書の長さに関わらず常に1ページしか出力されない不具合を修正。アプリ画面用の`html`/`body`の`height:100%; overflow:hidden`（画面全体を1ウィンドウ分に固定するための設定）が印刷時にも効いてしまい、1画面分を超えるコンテンツが切り捨てられていた。印刷用スタイルでコンテンツの実高さまで伸びるよう修正し、40セクションのテスト文書で14ページへ正しく分割されることを確認した。

### ドキュメント

- README、`document/`、配布用readme、HISTORY、LICENSEをQuickアプリテンプレートに合わせて統一。C++/WebView2版を出荷製品、Python版を評価用プロトタイプとし、UIは日本語のみ、第三者（PDFium、WebView2 Loader、Mermaid、MathJax）を明記した。
- ユーザー向け文書に「無料、広告無し、寄付無し、課金無し」を明記した。

## v1.0.0（2026-08-21）

### ネイティブ版（C++/WebView2）—製品として出荷される版

- 2度目のC++/WebView2移植を完了し、Python版プロトタイプと機能面でおおむね同等に到達。PDF切り出し、クロップ対応PNG/JPEG画像出力、キーボードショートカット、右クリックメニュー、Markdownプレビューモード（Mermaid図・MathJax数式対応）、MarkdownのPDF書き出し、PDF+Markdown混在選択時の警告を実装。
- `python/`（PySide6版）は開発中の挙動評価専用の試作品となり、配布はしない。

### デザイン

- QuickDiskBench/QuickFolderSizeと共通のModern Dark / Glassmorphismテーマへ変更。
- 新しいアプリアイコンを作成。
- アプリ内ヘルプメニュー（使い方ガイド・キーボードショートカット）とバージョン情報ダイアログを追加。

### ドキュメント

- README.md / README_jp.mdをネイティブ版が製品版であることを反映して全面刷新。
- `document/spec.md`・`spec_jp.md`・`about.md`・`about_jp.md`を新規作成。
- `dist/documents/readme.txt` / `readme_jp.txt`をネイティブ版の配布物・使い方（複数ファイルを開いた状態で保存すると1つのPDFへ連結される挙動を含む）を網羅する内容に刷新。

## 未リリース（2026-08-18）

### GUI

- PDF編集コアとUIを刷新。
- ページタグ表示、サムネイル並べ替え、Markdownプレビュー、バックグラウンド処理を改善。

### ドキュメント

- Quick系アプリ共通の文書・配布構成を適用。
