# AI Instructions & Rules (Claude, Codex, ChatGPT, Gemini, Grok)

本ファイルは、**すべての AI（Claude, OpenAI Codex / ChatGPT, Gemini, Grok, GitHub Copilot）がセッション開始時に自動で読み込み、ユーザーがいちいち過去の経緯や仕様を説明・指摘しなくても完全な文脈とルールを引き継ぐための絶対指示書** である。

---

## 🚨 【最高優先命令】ユーザーの指示は絶対であり、言い訳は一切許されない (Absolute Command & Zero Excuses)

1. **ユーザーの指示は絶対**:
   - ユーザーの指示・意図・ルール・制約事項は本リポジトリにおける最高絶対法である。例外なく 100% 服従すること。
   - AI 側の都合、勝手な自己判断・解釈で指示を曲げたり、省略したり、妥協することを固く禁ずる。
2. **言い訳の完全禁止**:
   - いかなる理由・技術的背景があろうとも、AI による言い訳、自己正当化、責任転嫁は一切許されない。
   - 不備・ミスが発生した場合は、言い訳を一切挟まず、直ちにユーザーの指示通りに完全修正すること。

---

## 🛡️ ポンコツ・杜撰ミス防止のための鉄則 (Zero-Tolerance Rules)

すべての AI は、以下の「杜撰なミス・ポンコツ行動」を絶対に起こしてはならない：

1. **勝手な思い込み（Assumption）の禁止**:
   - ユーザーの文章が書きかけの場合や指示が不完全な場合は、勝手に文章を完成させて投稿・コミットしない。必ず確認を取ること。
2. **勝手な捏造・AI生成での誤魔化し（Zero Hallucination）の禁止**:
   - ユーザーが「実機」「公式」の画像・データを求めているときに、AI生成で安易に代用・捏造しないこと。
3. **画像の自己検証（Self-Verification）の絶対義務化**:
   - 外部からダウンロードした画像やファイルは、ユーザーに提示・適用する前に **必ず自ら視覚的に中身を確認・検証すること**。関係のないデータを絶対に渡さないこと。
4. **中途半端なデプロイの禁止（再発防止の徹底）**:
   - サーバーやコンテナのコードを更新した際は、**必ずプロセス・コンテナを再起動し、プロセスメモリを完全更新すること**。メモリキャッシュが残ったままで放置し、不具合を再発させないこと。
5. **全プロセスの記録義務**:
   - 作業時は必ず企画・計画・実装・評価をドキュメントに記録・更新すること。
6. **言語設定**:
   - 思考・調査・報告・会話はすべて日本語で行うこと (すべて日本語で表示・思考)。
7. **WebView2アプリのプロセスを誤って落とさない**:
   - `msedgewebview2.exe` はこのアプリ専用のプロセス名ではない。Teams・Windows検索・他のWebView2アプリすべてが同じプロセス名を使う。
   - ビルド失敗（exeがロックされ書き込めない等）やテスト後のクリーンアップで、`taskkill /IM msedgewebview2.exe /F` のような**プロセス名だけを条件にした一括終了は絶対に実行しないこと**。無関係な他アプリ（Teams等）を巻き込んで落としてしまう。
   - 必ず対象アプリ自身の固有exe名（例: `QuickMarkPDF.exe`）でプロセスを特定し、そのPIDだけを `taskkill /PID <pid> /T /F`（プロセスツリーごと）で終了すること。`msedgewebview2.exe` を直接操作せざるを得ない場合は、`Get-CimInstance Win32_Process -Filter "Name='msedgewebview2.exe'"` 等でコマンドライン（`--user-data-dir` 等）を確認し、対象アプリのものだと確認できたPIDだけを終了すること。
