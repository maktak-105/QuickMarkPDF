# C++/WebView2 移植の中止記録（2026-08-19）

> **追記（2026-08-21）**: この記録は一度目の移植（破棄済み）についてのものである。
> 二度目の移植は 2026-08-19 に開始し、v1.1.0 時点で **C++17 + WebView2
> （`core/native/`）が出荷製品** である。`python/` は開発中の挙動評価用
> プロトタイプであり、配布しない。現行のビルド・位置づけは
> `document/environment.md` を読むこと。以下の本文は一次失敗の経緯と教訓の
> 記録として残す。移植が「継続中」という意味ではない。

C++17 + WebView2 への移植を試み、失敗したため**成果物を全て破棄した**。
このファイルは、次に着手する人が同じ失敗を繰り返さないための記録である。
再開時は、まずこのファイルだけを読めばよい。

破棄したもの: `core/native/`（C++実装）、`templates/` `static/`（WebView2 UI）、
`build_native.py` `build.bat` `bundle_html.py`（ビルド一式）、`third_party/`（PDFium/WebView2 SDK）、
`scripts/fetch_*.ps1`、`dist/binary/`、および移植関連の計画書・設計文書すべて。

残したもの: `python/`（動作するPySide6版。これが仕様の正）、`tests/`、`resources/`、`assets/`。

---

## 何が起きたか

移植は「ビルドが通り、自動テストが通れば完了」という基準で進めてしまった。
その結果、次の状態のものを繰り返し「実装済み」「完了」と報告した。

- サムネイル一覧の各ページに ↑ / ↓ / 回転 / 削除 のボタンを並べただけのUI
  （Python版にはそんなUIは存在しない。誰も指示していない）
- ドラッグ&ドロップによる並べ替えが無い
- プレビューペインが無い（ページをクリックしても何も起きない）
- サムネイルサイズが固定、スクロールバーすら無い
- 右クリックメニュー・設定画面・複数選択・ショートカットが無い

つまり Python版が持っていた機能・見た目・操作性のほとんどが入っていないまま、
「Phase 1 完了」等と報告し続けた。ユーザーの作業時間を一日分無駄にした。

さらに、UI以前の問題として **「PDFを開く」が一度も通っていなかった**。
原因は C++ 側が `PostWebMessageAsString`（JSには生のJSON文字列が届く）で応答していたのに、
JS 側が `event.data` をパース済みオブジェクトとして扱い `JSON.parse` を呼んでいなかったこと。
成功・失敗・キャンセルを問わず全ての応答が握り潰されていた。
この一行の欠落を、ダイアログのフォーカス問題や UIPI の疑いなど別の原因だと誤診し、
何日もそちらを調べ続けた。

## 次にやること

- **Python版（`python/`）を仕様の正とする。** 移植とは「Python版と同じ見た目・同じ機能・同じ操作性を再現すること」であり、それ以外は移植ではない。
- 着手前に、Python版の `ui/main_window.py` と `ui/thumbnail_panel.py` を読み、画面構成・操作・ショートカットを一覧化する。実装はその一覧を埋める作業として進める。
- 各段階で、**実際にアプリを起動し、実物のマウス・キーボードで操作して**動作を確認する。
- 「完了」と書くのは、上記の実操作確認が取れてから。

## 次にやらないこと

- **ビルド成功・自動テストPASSを「完了」の根拠にしない。** これらは前提条件であって、完了条件ではない。今回の失敗はここに尽きる。
- **未確認のものを「実装済み」「完了」と報告しない。** 確認できていない項目は、確認できていないと書く。
- **Python版に無いUIを勝手に足さない。** 特にサムネイル項目内へのボタン配置。
- **既存の土台に機能を継ぎ足す形で進めない。** 土台の構造がPython版と違う場合、継ぎ足しても永久にPython版にはならない。構造から合わせる。
- **ファイル選択ダイアログの自動操作に依存した検証をしない。** 何度試しても安定しなかった。検証のためにファイルを読み込ませたいなら、コマンドライン引数でPDFを開ける口を先に用意する（Python版は `main.py` で既に対応済み）。
- **PDFエンジンやモデル層を、UIの不満を理由に作り直さない。** 今回問題だったのはUIと検証の運用であって、そこではない。

## 技術メモ（再開時に踏み直さないために）

移植を再開するなら、以下は既知の落とし穴として先に潰しておくこと。

- WebView2 の `PostWebMessageAsString` は JS 側に**文字列**を渡す。受信側で `JSON.parse` が要る（`PostWebMessageAsJson` なら不要）。今回の最大の詰まりどころ。
- `add_WebMessageReceived` の第2引数は有効な `EventRegistrationToken*` が必須。`nullptr` を渡すと、WebView2 自体は起動するのにメッセージだけ一切届かない。
- `GetOpenFileNameW` は `OleInitialize` が必要。`CoInitializeEx` だけではダイアログが出ずに FALSE が返る。
- WebMessage コールバックから出すネイティブダイアログは前面に来ない。`AttachThreadInput` で前面スレッドと入力状態を共有してから `SetForegroundWindow` する必要がある。
- MinGW-w64 には `<wrl/event.h>` が無い。`Microsoft::WRL::Callback`/`ComPtr` は使えないので、COM コールバックは手書きする。
- メッセージ振り分けを部分文字列マッチで書かない。ファイルパスに他の命令名が含まれると誤爆する。`"type"` の値で分岐する。
